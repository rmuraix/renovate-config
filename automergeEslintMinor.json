{
  "packageRules": [
    {
      "extends": ["packages:eslint"],
      "labels": ["dependencies", "auto merge"],
      "automerge": true,
      "major": {
        "automerge": false,
        "labels": ["dependencies"]
      }
    }
  ]
}
